1. Упорядочть структуру IAR проекта (чтобы можно было развивать, поддерживать, передавать другим прграммистам)
2. Доавить в САПР (+ поддержка в прошивке)
 2.1 массивы строк (договорились в раздел "сообщения ALARM")
 2.2 динамический флаг (видимости/активности) экранных объектов
 2.3 прозрачный цвет для BMP картинок
 2.4 графики / тренды
 2.5 векторный формат картинок с возможнстью вращать/отражать/масштабировать
	- это может быть собственный формат (линии, полигоны, прямоугольники)
	- можно написать свой собственный мини-редактор + конвертер из стандартного формата (например svg)
	- на базе этих картинок + BMP уже можно составить библиотеку для разработчиков САПР-проектов (расширяемую)
 2.6 (может быть) список регистров контроллера для непрерывного опроса и складирования в указанные ячейки памяти
	- как переменные визуальных объектов и флаги ALARM
	- с привязкой к экрану
	- это может оптимизировать по скорости выполнение пользовательских программ
3. Требования от Сарбахтина и Микитянского
 3.1 авторизация операторов (логин / пароль)
 3.2 часы реального времени (на основе данных из контроллера). В САПР нужно указывать номер регистра вреиени
    	- возможно добавть графический примитив или формат строки (дата / время) согласованный
	  со стандартым форматом контроллера
	- журнал событий (ALARM + квинтирование) с привязкой к оператору
4. Дореализовать то, что уже есть в САПР но не поддерживается в прошивке
 4.1 Сообщения ALARM (но желательно уже расширенные массивами строк)
5. По САПР
 5.1 словарь переменных
 5.2 динамический цвет текста (готово)
 5.3 более удобные инструменты группового позиционирования (перемещения) объектов
	- например, желательна группировка (чтобы не выделять каждый раз десятки объектов)
	- копирование / вставка без смещения координат
	- перемещение с фиксированным шагом / вертикально / горизонтально
	- перемещение с явным указанием dX, dY

